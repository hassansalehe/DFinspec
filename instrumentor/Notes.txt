to instrument in tokens:
llvm.memcpy.p0i8.p0i8.i64
call void @llvm.memcpy.p0i8.p0i8.i64(i8* %7, i8* %10, i64 4, i32 1, i1 false)
  - memcopy takes 3 parameters: destination pointer, value value pointer, size of value.
  use size of value to determine nature of the token. copy the value to your manager.

 
To get the thread number use the library's "GetThreadID" function

adf_pass_token(void* outaddress, void* value ptr, unsigned long size) can be used to get out tokens.
outaddress is the address, ptr points to the value and size is length of the tokens
